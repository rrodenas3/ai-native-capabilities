# ai-native-capabilities — pinned requirements
# Last verified: June 2026
# Python: 3.13+
#
# DEPRECATION NOTES (critical):
#   - Claude 3.x: ALL retired by April 2026. Never use claude-3-* API strings.
#   - GPT-4o: retired February 2026. Never use gpt-4o strings.
#   - MCP SSE transport: deprecated March 2025. Use Streamable HTTP.
#   - LangGraph 0.x: use 1.0.5+. LangChain 0.x: use 1.0+.

# ── Orchestration ──────────────────────────────────────────────────────────────
# LangGraph 1.0.5 GA (Oct 2025) — stable, no breaking changes until 2.0
# Powers: stateful graphs, checkpointing, human-in-the-loop, streaming
langgraph>=1.0.5
langchain>=1.0.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langgraph-checkpoint-postgres>=2.0.0  # durable execution for Cap-04

# ── LLM providers ─────────────────────────────────────────────────────────────
# Anthropic — current model strings (June 2026):
#   claude-opus-4-8            (flagship)
#   claude-sonnet-4-6          (balanced, recommended default)
#   claude-haiku-4-5-20251001  (fast/cheap, subagent tier)
anthropic>=0.40.0

# OpenAI — current model strings (June 2026):
#   gpt-5.5                    (frontier, April 2026)
#   gpt-5                      (strong agentic baseline)
#   gpt-5-mini                 (cost-efficient)
openai>=1.60.0

# ── MCP ───────────────────────────────────────────────────────────────────────
# Spec 2025-11-25 (stable). Next RC: 2026-07-28 (stateless + Tasks)
# Transport: Streamable HTTP (production) or stdio (local dev)
# Auth: OAuth 2.1 + PKCE
mcp>=1.3.0

# ── Data & validation ──────────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── Vector store & retrieval ────────────────────────────────────────────────────
# pgvector 0.7.x — production-grade under ~50M vectors (HNSW + IVFFlat)
# pgvectorscale — higher throughput on same Postgres instance (Timescale)
pgvector>=0.3.0
psycopg[binary]>=3.2.0
pgvectorscale>=0.4.0
rank-bm25>=0.2.2            # lexical search leg of hybrid retrieval

# ── Memory ─────────────────────────────────────────────────────────────────────
redis>=5.2.0                # procedural memory (learned patterns, routing rules)

# ── Observability ──────────────────────────────────────────────────────────────
opentelemetry-api>=1.29.0
opentelemetry-sdk>=1.29.0
opentelemetry-exporter-otlp>=1.29.0
langsmith>=0.2.0            # deepest LangGraph integration; LangGraph Studio replay

# ── Security (mandatory — Cap-02 blocks on failures) ───────────────────────────
bandit>=1.8.0               # Python security linter
semgrep>=1.90.0             # pattern-based security scanning

# ── HTTP & utils ───────────────────────────────────────────────────────────────
httpx>=0.28.0
tenacity>=9.0.0
python-dotenv>=1.0.0
rich>=13.9.0
typer>=0.15.0
structlog>=25.1.0

# ── Dev & testing ──────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
fakeredis>=2.36.0
ruff>=0.9.0
mypy>=1.14.0

# ── Evals (installed separately; not required for core operation) ───────────────
# arize-phoenix>=8.0.0      # framework-agnostic RAG evals; OTEL-native; OSS
# braintrust>=0.0.190       # structured A/B eval runs; CI gates; 1M spans/month free
